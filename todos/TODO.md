# TODO.md（最新版本）

## 已完成

- ✅ 1. 修复同步数据误报「仅读取到缓存」
  - 目标：点击「同步数据」时不再立即误报仅读取到缓存；网络真实失败时才提示。
  - 用户价值：同步反馈准确，不再让用户误以为网络/代理有问题；同步引导也不会错误地保持打开。
  - 实现思路：不强制真读（用户明确反对，切号会卡）。缓存优先保持不变，`syncData` 在 `refetch()` 后等待同账号后台网络读取落定（`awaitScoreFresh` / `awaitChunithmFresh`，复用 in-flight 去重），再以最终缓存判定。
  - 依赖/风险：判定依赖 `readSettledGameDataBundle` 与 useGameData 使用同一 queryKey（已共享 `gameDataQueryKey` 单一来源）。
  - 当前优先级：高；状态：✅（2026-08-08 完成，见 `changelog/2026-08-08_修复同步误报仅读取缓存.md`）。

- ✅ 2. 中二新增 Rating 计算器与收藏品进度工具
  - 目标：补齐中二板块工具箱，实现「Rating / OVER POWER 计算器」与「收藏品进度」（称号/角色/名牌版/地图头像）。
  - 用户价值：中二玩家可直接计算单谱面 Rating/OVER POWER 与反推最低分数；对照落雪收藏品达成条件与完成状态，无需打开网页逐件查。
  - 实现思路：评分公式移植 refer/chuni-tools 并与既有 maxed 锚点互验；收藏品公共列表浏览 + 个人 API（`/user/chunithm/player/{type}/{id}`）登录后查询进度；两个页面均注册进 `game-toolbox`。
  - 依赖/风险：收藏品进度依赖落雪 OAuth 会话（现有中二账号已有）；公共列表不带 required，条件只能逐件查询。
  - 当前优先级：高；状态：✅（2026-08-08 完成，见 `changelog/2026-08-08_中二新增Rating计算器与收藏品进度工具.md`）。

- ✅ 3. Phigros 接入 Kyou 别名与谱面标签
  - 目标：把 Kyou 的歌曲别名与全部正票谱面标签接入 Phigros；别名用于详情和曲库/成绩搜索，标签用于详情以及曲库、成绩、随机歌曲筛选。
  - 用户价值：可以用社区常用别名查歌、查成绩，并按真实谱面配置组合筛选，减少跨站查询和手动辨认。
  - 实现思路：新增独立 Kyou Provider、Zod DTO、SQLite 快照和严格映射；按标题规范化、章节/定数组合消歧；标签筛选为同谱面 AND，并与详情难度卡片共用主标签决议及细分标签票数门槛，三处选择状态互相独立，随机偏好升级 v3。
  - 依赖/风险：依赖 Kyou `latest` JSON 契约与对象存储可用性；网络或结构失败回退缓存，歧义映射只跳过不猜测；不改变个人标签和备份格式。
  - 当前优先级：高；状态：✅（2026-08-09 完成；同日修复 `+N` 弹层手势冲突、强化主次标签区分，并按票数规则归纳主标签、裁剪细分标签及扩大卡片内点击热区，随后统一筛选器与详情难度卡片的票数条件，见 `changelog/2026-08-09_Phigros接入Kyou别名与谱面标签.md`、`changelog/2026-08-09_修复Phigros谱面标签展开与主次区分.md`、`changelog/2026-08-09_调整Phigros标签票数规则与展开交互.md` 与 `changelog/2026-08-09_Phigros难度标签筛选票数条件.md`）。

- ✅ 6. Phigros 新增实力分析工具
  - 目标：按玩家 RKS 动态生成高水平谱面分析池，以五维主标签雷达和细分标签 RKS 展示各方面强弱。
  - 用户价值：玩家无需逐曲手工归类，即可定位读谱、耐力、协调、手速、多指及具体配置的优势与短板。
    - 实现思路：取 `min(floor((玩家精确 RKS - 0.2) × 10) / 10, 16.0)` 为阈值；候选池包含定数达标的全部谱面并独立决定稀缺系数与候选均定，基础分析池保留 RKS 达标且评级 A 以上的成绩；普通谱面归入最高票主标签，综合谱面归入前两项，细分标签要求票数大于 3；标签基础样本为 1–2 张时，从候选池内已有成绩但未进入基础分析池的同标签谱面按 RKS 降序补入最多 5 张，补入成绩参与标签 RKS、覆盖率及歌曲列表，但不改变候选数量和稀缺系数；标签原始 RKS 使用不加权算术平均，完整难度校准为“同类最高候选平均定数 / 当前候选平均定数”，其增量按“分析样本数 / 候选数”覆盖率渐进生效；随后对未达到基准的结果应用 `1.0000–1.0200` 候选数量补偿并以满分基准封顶，确保全满时五维严格相等；主标签与细分标签分别计算；主标签候选覆盖率归一化为占比后生成均衡型、双核型、特化型或倾向型简评，并以“分析：{评价}”居中展示；以相对最弱主标签为练习方向，逐张模拟候选谱面在 `0.01%` Acc 精度下对标签修正 RKS 的影响，排除满分仍无法带来至少 `0.0001` 提升的谱面，再按定数从低到高推荐 3 张并给出最低目标 Acc；工具页提供动态刻度雷达、持平/强弱摘要、薄弱项练习、细分排行、原始值/系数/候选均定/覆盖率、样本数和缓存/异常状态，薄弱项练习与细分标签标题不显示冗余数量，顶部分析池规则默认单行截断且可展开/收起，标签歌曲列表及练习推荐均可直接进入对应歌曲详情难度卡片。
  - 依赖/风险：依赖 Phigros TapTap 成绩、曲库及 Kyou 五项主标签结构；缓存可继续分析并提示过期，主标签结构异常时停止绘图以避免错误结论；不修改 Provider、存储或备份格式。
    - 当前优先级：高；暂缓原因：无，用户已明确要求实现；状态：✅（2026-08-09 完成，并根据真机反馈收敛雷达文字布局，将标尺确定为“入池阈值—最高主标签 RKS + 0.1”，主标签区域会居中显示“分析：{评价}”形式的候选覆盖占比简评，顶部分析池规则默认单行截断并提供展开/收起按钮；所有主/细分标签均可打开同口径达标谱面列表，薄弱项练习会从相对最弱主标签中筛出真正可提升且定数最低的 3 张谱面、给出最低目标 Acc，并可直接定位歌曲详情难度卡片；入池阈值最高为 16.0，并加入带覆盖率收缩及满分基准封顶的候选谱面数量补偿与平均定数难度校准；标签基础分析样本为 1–2 张时，可从定数达标候选池中按 RKS 向下补入最多 5 张已有成绩，且候选数量与稀缺系数保持独立不变，确保全满账号五维等值且低覆盖标签不会过度膨胀；2026-08-10 移除薄弱项练习与细分标签标题右侧的冗余数量，见 `changelog/2026-08-09_Phigros实力分析工具.md`、`changelog/2026-08-09_修复Phigros实力分析雷达布局.md`、`changelog/2026-08-09_校准Phigros实力分析雷达刻度.md`、`changelog/2026-08-09_Phigros实力分析标签歌曲列表.md`、`changelog/2026-08-09_Phigros实力分析阈值上限.md`、`changelog/2026-08-09_Phigros实力分析加入稀缺系数.md`、`changelog/2026-08-09_Phigros实力分析加入难度校准.md`、`changelog/2026-08-09_校准Phigros全满五维等值.md`、`changelog/2026-08-09_收敛Phigros低覆盖标签系数.md`、`changelog/2026-08-09_Phigros样本较少标签向下补充.md`、`changelog/2026-08-09_Phigros实力分析列表跳转难度卡片.md`、`changelog/2026-08-09_Phigros主标签占比简评.md`、`changelog/2026-08-09_优化Phigros实力分析标题与说明.md`、`changelog/2026-08-09_Phigros实力分析薄弱项推荐.md` 与 `changelog/2026-08-10_移除Phigros实力分析标题计数.md`）。

- ✅ 7. Phigros 全曲全谱面满成绩示例账号
  - 目标：参照舞萌与中二节奏板块，为 Phigros 增加可添加、切换、删除和恢复的全满示例账号。
  - 用户价值：无需 TapTap 登录即可检查 Phigros 总览、成绩、曲库、Best30 与工具页面在完整满成绩数据下的表现。
  - 实现思路：新增独立 `phigros-test` 提供方，按当前 Phigros 曲库动态生成全谱面 1,000,000 分、100% Acc、AP、Phi 成绩；按正式 Phi3/Best27 口径计算玩家 RKS；课题模式最高分取定数最高的三个谱面分别向下取整后相加；以独立本地 KV 档案保存示例账号是否存在。
  - 依赖/风险：动态成绩依赖现有 Phigros 曲库资源；不伪造 TapTap 会话、不修改云存档、Provider DTO、备份 Schema 或版本号。
  - 当前优先级：高；暂缓原因：无，用户已明确要求实现；状态：✅（2026-08-09 完成并校准课题模式最高分，见 `changelog/2026-08-09_Phigros添加全满示例账号.md` 与 `changelog/2026-08-09_校准Phigros示例账号课题模式.md`）。

- ✅ 8. 舞萌段位认定接入与 Phigros 别名显示修复
  - 目标：读取 LXNS 与水鱼的舞萌段位认定，在总览 Rating 卡和两种舞萌成绩图片中展示；同步调整牌子进度说明，并修复 Phigros 歌曲详情别名布局。
  - 用户价值：不同舞萌数据源都能准确展示段位，生成图片保留完整玩家身份信息；Phigros 别名与其它板块保持一致的单行展示及按需展开体验。
  - 实现思路：在各自 Provider 边界把 `course_rank` / `additional_rating` 归一化到舞萌段位素材编号并写入类型化 `extension`，由统一展示函数输出名称和素材；Rating 卡复用侧徽章，游戏风格图片复用 `DaniPlate` 位置，应用风格在玩家姓名旁显示段位标签；Phigros 别名改为隐藏测量溢出后再显示展开/收起。
  - 依赖/风险：依赖 LXNS `course_rank` 与水鱼 `additional_rating` 的既有编号契约；保留水鱼原始字段兼容旧快照，不修改存储或备份格式；别名是否溢出由真实文本布局回调决定。
  - 当前优先级：高；暂缓原因：无，用户已明确要求实现；状态：✅（2026-08-10 完成，见 `changelog/2026-08-10_舞萌段位与Phigros别名修复.md`）。

- ✅ 9. 舞萌示例账号段位设为裏皆伝
  - 目标：将舞萌全满示例账号的段位认定从初学者调整为最高段位“裏皆伝”。
  - 用户价值：示例账号的全满成绩、Rating 与段位状态保持一致，便于检查总览和成绩图片的最高段位展示。
  - 实现思路：在示例 Provider 的玩家 `extension.courseRank` 中固定输出最高素材编号 23，并让段位展示映射使用用户指定的“裏皆伝”名称。
  - 依赖/风险：仅改变生成式示例账号，不影响 LXNS、水鱼、本地账号、存储或备份数据；真实账号同编号也会使用统一的“裏皆伝”显示名称。
  - 当前优先级：高；暂缓原因：无，用户已明确要求实现；状态：✅（2026-08-10 完成，见 `changelog/2026-08-10_舞萌示例账号段位设为裏皆伝.md`）。

- ✅ 10. 舞萌应用风格成绩图段位改为姓名旁标签
  - 目标：将应用风格成绩图片中的段位认定从独立大徽章改为玩家姓名旁的紧凑标签。
  - 用户价值：玩家资料头部更接近总览页的信息层级，段位不会抢占过多横向空间或成为独立视觉区块。
  - 实现思路：把段位标签移入 Rating 身份卡的姓名行，使用“段位认定 + 段位名称”的单个胶囊标签；恢复身份卡可用宽度，并在长姓名自适应计算中扣除标签宽度。
  - 依赖/风险：只调整应用风格成绩图片；游戏风格仍使用原有 `DaniPlate` 位置，段位数据来源与映射不变；长姓名继续动态缩放而不省略。
  - 当前优先级：高；暂缓原因：无，用户已明确要求实现；状态：✅（2026-08-10 完成，见 `changelog/2026-08-10_舞萌应用风格成绩图段位改为姓名旁标签.md`）。

- ✅ 11. 舞萌工具箱新增 KALEIDX◈SCOPE 万花筒工具
  - 目标：在舞萌工具箱和首页快捷入口接入 `KALEIDX◈SCOPE`，离线展示截至 2026-08-10 已开放的蓝、白、紫、黑、黄、红六色门资料，并提供符合真实条件的手动进度记录。
  - 用户价值：玩家可集中查询六门开放日期、解锁步骤、钥匙曲池、挑战曲池、固定门曲、完美挑战曲及难度/LIFE 缓和阶段，并按账号维护辅助进度，无需反复翻查公告和攻略。
  - 实现思路：新增类型化静态数据注册表、六色门查询页面和独立版本化 KV 状态；蓝/黑/红记录全曲清单，白/紫限制单人 3 首或多人 4 首的不重复单局计划，黄门记录单次随机命中；曲库命中时提供曲绘和歌曲详情跳转，未命中仍保留静态资料；补充数据、存储、状态、工具箱和页面测试。
  - 依赖/风险：静态资料以舞萌 DX 官方公告与 AWMC 国服攻略仓库为主要来源，核对日期固定为 2026-08-10，后续国服规则变化需更新注册表；应用内进度仅为手动辅助记录，不替代机台状态；不修改 Provider、用户曲库、备份格式或版本号。
  - 当前优先级：高；暂缓原因：无，用户已明确要求实现；状态：✅（2026-08-10 完成，见 `changelog/2026-08-10_舞萌新增KALEIDXSCOPE万花筒工具.md`）。

## 暂缓

- ⏸️ 4. Phigros 同步等待后台刷新落定
  - 目标：Phigros「同步数据」也等待 TapTap 云存档读取真正完成后结束「同步中…」。
  - 用户价值：与其他游戏同步反馈一致；当前行为是同步瞬间返回、后台静默刷新。
  - 实现思路：为 Phigros 的 `cacheFirstLoad` 分支暴露类似的落定等待（现有 `PhigrosSaveCache` 无 in-flight 注册）。
  - 依赖/风险：需新增一个服务级注册点；无可见错误提示，优先级低。
  - 当前优先级：低；暂缓原因：非用户报告问题，无可见误报，等待有真实需求时再实现。

- ⏸️ 5. 中二 Rating 趋势 / 成绩热力图 / 最近游玩
  - 目标：复用落雪个人 API 的 trend / heatmap / recents 端点做可视化与最近记录。
  - 用户价值：查看 Rating 随时间变化与游玩活跃度；最近游玩 50 条便于回看。
  - 实现思路：新增 `ChunithmScoreProvider` 读取端点与对应页面。
  - 依赖/风险：trend/heatmap 文档标注需要 `allow_third_party_fetch_history` 权限，当前 OAuth scope（read_user_profile/write_player/read_player）可能不覆盖，需先向落雪确认；recents 也需要同权限域验证。
  - 当前优先级：低；暂缓原因：权限未确认，避免假定上游能力存在；待用户向落雪申请后实现。
