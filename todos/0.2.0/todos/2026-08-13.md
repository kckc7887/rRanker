# 2026-08-13 待办

## 173. ✅ ADOFAI 成绩卡与歌曲详情二次重构

- 目标：按应用现有歌曲详情结构重新整理 ADOFAI 最佳、成绩和关卡详情；成绩卡只保留 Score、XACC、倍速、底部难度与居中 Impact，详情使用方形真实封面、简要信息栏、单难度成绩卡、标签图标和七项判定矩阵。
- 用户价值：高频成绩信息能够一眼扫读，单曲详情与舞萌、中二、Phigros、喵斯的页面层级一致，同时保留 ADOFAI 独有的 PGU 光谱难度和快慢判定语义。
- 实现思路：继续复用公共列表、`GameScoreCard`、`GameChartResultCard`、`SongMetadataTable`、`AutoScrollText`、个人曲库和媒体回退路径；新增关卡公开成绩只读查询并按当前 PID 精确选择 Score V2 最佳记录；难度使用 TUF 原始色值；标签图标固定到 TUFHelper 提交，未映射时显示文字。
- 依赖/风险：关卡成绩依赖 TUF `/v2/database/passes/level/{id}`；标签图标依赖固定 GitHub Raw 提交。查询失败只影响成绩卡，图片失败沿既有候选回退，不修改 SQLite、备份、路由或版本号。
- 当前优先级：高。
- 暂缓原因：不暂缓，本轮已完成并根据用户复核再次收敛公共布局（见 `changelog/2026-08-13_ADOFAI成绩卡与歌曲详情二次重构.md`、`changelog/2026-08-13_ADOFAI公共布局二次收敛.md`；lint 0 error、typecheck 通过、Vitest 141 文件 955 例通过、Jest 53 套件 310 例通过；固定 390×844 Host Tree/Style 验证详情头图为 390×390、顶部 inset 关闭、溢出文本可滚动）。

## 174. ✅ ADOFAI 成绩与曲库筛选统一

- 目标：让 ADOFAI 成绩页与曲库页使用同一组 PGU 难度段、1–20 难度区间和特殊难度开关；各自保留原排序项与升降序，成绩页增加 WF、PP 筛选。
- 用户价值：两页筛选操作一致，用户无需记忆两套控件；收起态只概括真正生效的筛选，WF/PP 也能快速定位并保持与其它板块一致的金色成就视觉。
- 实现思路：在既有 `TufFilterBar` 内抽取共用难度筛选组合；曲库继续使用 TUF 关卡接口的 `pguRange` 与 `specialDifficulties`，成绩因上游接口不接受这些条件而复用现有分页查询，在筛选激活时读完公开分页后由 TUF 领域函数筛选；WF/PP 使用公共 `LayeredGradientBadge` 金色主题。
- 依赖/风险：成绩筛选需要完整读取当前查询的公开成绩分页，分页失败时停止自动续取以避免重试循环；不修改缓存键、存储、路由、备份或版本号。
- 当前优先级：高。
- 暂缓原因：不暂缓，本轮已完成并验证（见 `changelog/2026-08-13_ADOFAI成绩与曲库筛选统一.md`；lint 0 error、typecheck 通过、Vitest 141 文件 957 例通过、Jest 53 套件 311 例通过）。

## 175. ✅ 解锁 ADOFAI / 喵斯工具箱与固定成绩图

- 目标：为 ADOFAI、喵斯开放随机歌曲、机厅查找和成绩图片工具；机厅查找不携带游戏筛选；ADOFAI 从公开成绩每关最佳随机，喵斯从全曲库随机；最佳页分别提供 Top20、B30 固定成绩图入口；同时修复 TUF 真实头像字段和旧账号空头像补全。
- 用户价值：两个社区游戏板块获得与现有游戏一致的工具入口和随机练歌流程，可直接从最佳页导出适合分享的应用风格成绩图；旧绑定 TUF 玩家无需重新绑定即可恢复头像。
- 实现思路：工具入口继续由 `GAME_TOOLBOXES` 注册并由 `GameProfile` 推导；随机抽样复用 `RandomChartsPage`、`pickRandomItems`、现有筛选器和成绩卡，ADOFAI 自动读完 `bestPerLevel=true` 分页并按关卡去重，喵斯用曲库谱面与成绩可选 join；固定图通过不枚举游戏 ID 的 `ScoreCardPresentation` HTML 构建器和公共 WebView 导出外壳实现；TUF 头像统一经 `resolveTufAvatarUrl` 解析并接入缓存优先补全、会话更新和头像/缩略快照。
- 依赖/风险：依赖 TUF 公开资料/成绩分页与 MuseDash.moe 曲库、成绩和 `/rank` 明细；明细未就绪时 FC/AP 候选暂不进入池。固定图远程头像失败回退本地 ADOFAI 图标；不修改 Provider 地址、版本号、账号存储 Schema、备份格式或既有游戏视觉参数。
- 当前优先级：高。
- 暂缓原因：不暂缓，本轮已完成并验证（见 `changelog/2026-08-13_解锁ADOFAI与喵斯工具箱和固定成绩图.md`；lint 0 error、typecheck 通过、Vitest 142 文件 967 例通过、Jest 53 套件 313 例通过；1080px 固定 HTML 实际渲染为三列，ADOFAI 20 卡高 1950px、喵斯 30 卡高 2638px，横向无溢出）。

## 176. ✅ 修复 ADOFAI 随机加载与两游戏成绩图公共链路

- 目标：让 ADOFAI 随机页在首批成绩到达后立即可见，以最多三路并发准备完整且每关唯一的随机池；统一五个最佳页的导出按钮；修复 ADOFAI Top20 与喵斯 B30 的预览空白、导出截断和非应用风格问题。
- 用户价值：用户无需等待串行读取全部 TUF 分页才看到页面，并且只会在完整无偏池准备好后抽取；五个游戏的最佳页入口一致，固定成绩图可正常预览、完整保存并保持应用内成绩卡的信息层级。
- 实现思路：新增无游戏分支的有限并发 offset 分页加载器，TUF 继续复用既有 Provider、Schema、React Query key 与缓存优先页加载；失败只重试对应 offset，账号或查询变化用 AbortSignal 隔离。五个入口复用同一 46px 主题实底按钮。固定成绩图由两游戏共用 `FixedBestImageScreen`、WebView 状态函数、资源准备、截图和保存函数，预览固定走内联 HTML、Android 导出固定走临时文件；HTML 共用 `.preview-stage/.canvas/data-layout-content` 运行时和 `ScoreCardPresentation` 映射，不在模板或控制器中判断游戏 ID。
- 依赖/风险：完整随机池仍依赖 TUF 公开分页，失败页未恢复前禁止抽取；远程头像和封面继续走既有回退。当前机器没有连接 Android 设备或模拟器，原生分流由 Jest 合同覆盖，完整画布由 Chromium 固定视口验证；不修改 Provider、算法、存储、备份、版本号或其它游戏视觉参数。
- 当前优先级：高。
- 暂缓原因：不暂缓，本轮已完成并验证（见 `changelog/2026-08-13_修复ADOFAI随机加载与两游戏成绩图公共链路.md`；lint 0 error、typecheck 通过、Vitest 143 文件 970 例通过并跳过 2 文件 2 例、Jest 55 套件 317 例通过；1080px Chromium 实际渲染为三列，ADOFAI 20 卡与底部署名完整包含在 1483px 画布内，喵斯 30 卡与底部署名完整包含在 1987px 画布内，均无横向溢出）。

## 177. ✅ 重做 ADOFAI TopN / 喵斯 BestN 应用风格成绩图

- 目标：删除固定成绩图中自行添加的顶部装饰线、白色通用卡片、右侧指标栏和独立条数文案，以 Phigros 现有“应用风格”为唯一视觉合同；ADOFAI 与喵斯只映射各自身份、成绩、难度主题、封面和标签数据，并修复预览空白、导出不完整及 TUF 头像未显示。
- 用户价值：导出的 TopN / BestN 与应用内成绩卡及其它板块成绩图保持同一信息层级，不再出现自创排版；头像、封面和图标在离线 HTML 中稳定显示，预览与相册导出使用同一完整页面。
- 实现思路：抽取无 `gameId` 的 `ApplicationBestImageCardPresentation`、Phigros 三列卡片渲染器/CSS 与公共画布 runtime，Phigros 自身改走公共实现并保持像素不变；两游戏薄适配器严格输出指定主指标、关系行、图标行和两行徽章。远程素材复用既有 expo-image 磁盘缓存转 data URI，TUF 视频封面继续复用 `tufVideoDetailsQueryOptions` 的 Provider/React Query key，头像按 `pfp → user.avatarUrl → avatarUrl → avatar` 后回退本地 ADOFAI 图标。`FixedBestImageScreen` 的预览与导出统一使用 `prepareBestImageWebViewSources` 生成的同一个本地 HTML source。
- 依赖/风险：TUF 视频详情、头像和图标及 MuseDash.moe 封面依赖远程素材；单项失败按卡片回退，不阻断整图。当前机器 `adb devices` 无连接设备，因此没有宣称 Android/iOS 原生实机验证；WebView 协议与截图链路由 Jest 合同覆盖，完整画布由本地 Chrome 固定视口检查。
- 当前优先级：高。
- 暂缓原因：不暂缓，本轮已完成并验证（见 `changelog/2026-08-13_重做ADOFAI与喵斯应用风格成绩图.md`；lint 0 error/26 条既有 warning、typecheck 通过、Vitest 145 文件 977 例通过并跳过 2 文件 2 例、Jest 55 套件 317 例通过；1080/1440/2160 均为三列且无横向溢出，ADOFAI 20 卡画布高度分别为 1960/2611/3898px，喵斯 30 卡为 2672/3560/5316px，底部署名完整；Phigros 三档截图差异像素均为 0）。

## 178. ✅ 修复喵斯与 ADOFAI 成绩图预览空白与素材整屏等待

- 目标：去掉「全部封面本地化完成才出现预览」的整屏硬门槛，预览窗进入即渲染、封面远程 URL 先出、本地化进度显示在预览窗下方；不新建任何加载器，HTML 目录迁移到 Documents/rranker 对齐舞萌/Phigros。
- 用户价值：进入成绩图页面不再干等素材，预览与其它板块一致地即时可见并渐进填充封面；本地化完成后自动切换离线版本保证导出完整。
- 实现思路：两社区屏幕删除素材整屏阻塞，卡片在素材未就绪时用既有 domain URL 构建器（museDashCoverUrl/tufMediaImageCandidates/resolveTufAvatarUrl/tufTagIconUrl）远程先出，就绪后重建为既有共享 data URI；本地化失败静默保留远程兜底；`FixedBestImageScreen` 经既有 `prepareBestImageWebViewSources` 目录参数写入 `Documents/rranker/best-image-pages` 并新增预览框下进度行；共享卡片渲染器封面 img 补舞萌/中二同款 onerror 兜底（Phigros file 图源永不失败，零视觉变化）。
- 依赖/风险：远程先出阶段封面依赖网络与 TUF 媒体接口，失败项按卡片 ♪ 兜底；本地化仍走既有 data URI 链路（中二同款）。当前机器无 adb 设备，原生渲染由 Chromium 真实产物 + 真实远程 webp 验证。
- 当前优先级：高。
- 暂缓原因：不暂缓，本轮已完成并验证（见 `changelog/2026-08-13_修复喵斯与ADOFAI成绩图预览空白与素材整屏等待.md`；lint 0 error/26 条既有 warning、typecheck 通过、Vitest 145 文件 977 例通过并跳过 2 文件 2 例、Jest 55 套件 318 例通过；Chromium 验证远程先出 HTML 约 50KB、30 卡齐全、真实 webp 15 张加载成功、失效 URL 15 张被 onerror 隐藏、导出视口无缩放偏移）。
