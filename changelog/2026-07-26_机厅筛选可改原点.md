# 机厅筛选可改原点

## 改动原因

用户希望在筛选器中修改搜索原点，而不是始终只能用当前 GPS。

## 具体实现

1. 筛选器新增「原点」行：显示当前原点文案，提供「当前位置」「搜索地址」。
2. 「搜索地址」打开应用内 pageSheet，经 `expo-location` 地理编码后设为自定义原点。
3. 列表按当前原点坐标请求 nearcade discover；改距离或原点后重新加载。
4. 重置筛选会回到 GPS 原点；收起摘要包含原点标签。

## 期望输出

可在筛选器切换 GPS / 自定义地址原点，并据此刷新附近机厅列表。

## 实际输出

与期望一致。
