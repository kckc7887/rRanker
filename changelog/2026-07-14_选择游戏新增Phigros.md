# 选择游戏列表新增 Phigros

## 改动原因

用户要求在选择游戏列表中新增 Phigros，并提供了游戏图标。

## 具体实现

- 写入 `assets/images/phigros.png`。
- 将选择表改为多游戏数据模型：`舞萌 DX`（可展开查分器）与 `Phigros`（待实现，点击提示尚未开放）。
- 展开状态改为按游戏 ID 管理，同组内两条游戏之间使用分隔线。
- 在 `TODO.md` 记录 Phigros 成绩绑定暂缓项；单测断言列表出现 Phigros。

## 期望输出

选择游戏表同时展示舞萌 DX 与 Phigros；Phigros 显示待实现状态，不进入绑定流程。

## 实际输出

按上述实现；`m3-settings` 三项用例通过。
