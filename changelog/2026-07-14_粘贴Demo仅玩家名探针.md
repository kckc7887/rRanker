# 粘贴 Demo 改为仅取玩家名即停

## 改动原因
App 提示无 Location；用户要求 Demo 抓取时只要拿到玩家名字就立刻停止，便于快速验证换会话。

## 具体实现
- `oauth_paste_gui.py`：`wechat` 换 cookie 后只调 `players`，读到 `name` 即停止，不再 `scores`
- 主按钮改为「仅取玩家名」；输出 `last_player.json`

## 期望输出
粘贴新鲜最终链接后弹出玩家名并停止。

## 实际输出
Demo 已改为探针模式。
