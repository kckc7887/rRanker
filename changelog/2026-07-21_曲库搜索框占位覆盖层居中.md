# 曲库搜索框占位改用覆盖层居中

## 改动原因

仅靠 TextInput 样式（`textAlignVertical` / `includeFontPadding`）仍无法在当前环境下把占位文字垂直居中。

## 具体实现

曲库搜索改为外层固定高度容器 + 绝对定位 `Text` 绘制占位（`lineHeight` 等于容器高度），输入框本身留空 placeholder，保证提示文字真正上下居中。

## 期望输出

空搜索框时提示文字在框内垂直居中；输入后提示消失、输入文字正常显示。

## 实际输出

已接入 `CatalogSearchField` 覆盖层占位方案。
