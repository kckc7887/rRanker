# 2026-07-30 修复谱面确认卡在准备中

## 改动原因

谱面确认页一直停在「准备中」：`file://` WebView 上大段内联脚本与 `injectedJavaScriptBeforeContentLoaded` 不可靠，播放器拿不到参数或不执行。

## 具体实现

- 播放器改为独立 `player.bundle` → 落盘为 `player.js`，HTML 用 `<script src="./player.js">`。
- 准备资源时把 `chartId/difficulty/title` 直接写入 HTML；`onLoadEnd` 再注入一次作为兜底。
- 播放器等待参数时显示「正在等待参数…」，并用 `Number.isFinite(chartId)` 判定。

## 期望输出

进入谱面确认后能越过准备中，开始加载谱面/预览曲。

## 实际输出

相关单测与 typecheck 通过；需真机再进一次确认页验证。
