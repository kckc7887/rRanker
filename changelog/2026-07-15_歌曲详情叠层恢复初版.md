# 2026-07-15 歌曲详情叠层恢复初版

## 改动原因

用户要求回到第一次修复返回键时的方案与外观，放弃后续 headerLeft / 液态玻璃 / Blur 正圆迭代。

## 具体实现

1. 恢复透明沉浸标题栏，并关闭原生左右按钮。
2. 页面叠层 `SongDetailChrome`：RN `Pressable`、40×40 命中区；Android 半透明圆底，iOS 仅图标。
3. 移除 `headerShown: false`、`BlurView` 圆形玻璃钮。

## 期望输出

- 与首次返回键修复版本一致：叠层可点返回/收藏，沉浸式曲绘保留。

## 实际输出

- `songs/[songId].tsx` 已按初版叠层恢复；既有返回/收藏 Jest 断言继续适用。
