# 上传页去掉粘贴框，只展示爬取状态

## 改动原因
用户指出不应再放「复制最终链接」输入框；代理截包路径下只需展示爬取/处理状态。

## 具体实现
- `UploadDataSheet` 移除粘贴框、粘贴上传按钮（保留授权链接复制见后续 changelog）
- 保留截包 IP:11451、处理服务 :18765 配置
- 轮询电脑 `GET /v1/status` 展示状态 / 玩家名 / 错误

## 期望输出
上传页仅为代理配置 + 状态面板。

## 实际输出
UI 已按此精简。
