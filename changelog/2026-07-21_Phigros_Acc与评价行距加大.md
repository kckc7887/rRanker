# Phigros Acc 与评价行距再加大

## 改动原因

上一轮仅加了 6px 底边距，Acc 上下限输入框与下方评价下拉仍过近。

## 具体实现

将 Acc 行 `marginBottom` 从 6 调整为 14（叠加筛选栏原有 `gap: 10`，与评价行视觉间距约 24）。

## 期望输出

Acc 输入框与评价下拉之间有明显空隙，不再贴合。

## 实际输出

已加大 Acc 行底边距。
