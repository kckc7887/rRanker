# 手机端对齐 Go/已验证路径复现微信换会话抓取

## 改动原因
用户确认 PC 上 `maimai_py` 可出完整成绩；要求在手机用自研 TS 复现（不嵌闭源库）。此前 App 用 `redirect:follow` 直撞回调，易 404/会话失败。

## 具体实现
- `wechat-oauth`：改用 Windows 微信 UA；解析不再整串 decode。
- 新增 `wechat-http`：手动 Cookie 罐（`_rawHeaders` / `getSetCookie`）。
- `wechat-score-crawler`：首跳 `redirect:manual` 收 302+Cookie → 二次跳转 → 显式 Cookie 串行抓五难度。
- 文案与 TODO 同步；单测覆盖 Cookie 罐。

## 期望输出
总览上传：粘贴新鲜最终链接后可换会话、抓成绩并推水鱼（须 Import-Token）。

## 实际输出
代码已接线；需真机验证 expo/fetch 的 manual redirect 与 Set-Cookie 可见性。
