# 上传页补充接到 callback 后的行为说明

## 改动原因
用户指出上传页只写了代理 IP/端口与分流，没有写截到链接之后要做什么。

## 具体实现
- `UploadDataSheet` 增加「接到未消费 callback 之后」步骤：解析四参数 → NO_PROXY 换 cookie → 爬五难度 → 水鱼上传 → 再同步本机；并注明代理须放行流出、勿二次经 mitm。

## 期望输出
打开上传页即可看到截包配置与接到链接后的完整行为链。

## 实际输出
文案区块已落地。
