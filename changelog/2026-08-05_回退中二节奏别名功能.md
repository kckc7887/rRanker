# 2026-08-05 回退中二节奏别名功能

## 改动原因

中二节奏别名功能（提交 213d954 接入别名与歌曲信息卡调整、3a23c69 别名修复）在实测中别名不显示、按别名搜索无结果。用户要求回退本次更改，不新增任何内容、不动公共逻辑。

## 具体实现

- `git reset --hard 8846f0d`：回退 213d954 与 3a23c69 两个本地提交（未推送），删除别名相关代码与两个 changelog 文件。
- 恢复内容：`ChunithmSong` 无 `aliases` 字段、`ChunithmCatalogProvider` 无 `getAliases`、`useChunithmCatalog` 恢复为单一 queryFn、中二详情页恢复原「歌曲与版权信息」卡（含 LXNS/SEGA 版权声明，位于本地标签卡下方）、中二搜索恢复不含别名的索引。

## 期望输出

- 中二板块恢复接入别名前的完整原状，舞萌侧不受影响。

## 实际输出

- `git status` 干净，HEAD 位于 8846f0d，中二相关 12 个文件全部还原；未执行 typecheck/测试（改动仅为还原既有代码）。
