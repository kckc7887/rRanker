# 从仓库移除 demo 目录

## 改动原因

Demo 为本地概念验证，不应出现在远端仓库；此前仅忽略部分内容并保留 `demo/README.md`，现改为整目录不进版本库。

## 具体实现

- 停止跟踪 `demo/` 下已纳入版本库的文件并从仓库树中移除。
- 本地 `demo/` 目录本身保留，不删除工作区文件。

## 期望输出

远端与正式仓库树中不再包含 `demo/`；本地 Demo 文件仍可继续使用。

## 实际输出

`demo/` 已从版本库索引移除，后续不再提交该目录内容。
