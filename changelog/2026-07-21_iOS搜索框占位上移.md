# iOS 搜索框占位上移

## 改动原因

用户在 iOS 上反馈搜索框提示仍偏下；此前主要按安卓调整，iOS 仍用对称 padding。

## 具体实现

1. 抽出共用 `SEARCH_BOX_STYLE`。
2. iOS 使用 `paddingTop: 6` / `paddingBottom: 14`，把占位文字上移光学居中。

## 期望输出

iOS 曲库/成绩搜索框提示文字不再明显偏下。

## 实际输出

已按 iOS 不对称 padding 调整并统一两页样式。
