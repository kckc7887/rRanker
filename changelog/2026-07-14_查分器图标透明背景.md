# 查分器图标去掉底色

## 改动原因

用户要求选择游戏列表中的图标不要底色，使用透明背景。

## 具体实现

- 移除查分器图标外层色底容器与 `iconTone` 字段。
- 舞萌 / 水鱼 / 落雪图标均直接以透明底的 `Image` 展示，不再附加 `#111827` / `#F3F4F6` 背景。

## 期望输出

列表图标四周无额外色块，只显示图标内容本身。

## 实际输出

UI 色底已去掉。若个别 PNG 资源自带黑底，仍取决于素材文件本身。
