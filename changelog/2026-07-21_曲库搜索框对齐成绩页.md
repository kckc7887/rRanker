# 曲库搜索框对齐成绩页写法

## 改动原因

覆盖层占位方案使提示更偏下；回退后按成绩页已验证可用的搜索框样式对齐。

## 具体实现

1. `git revert` 撤销覆盖层居中提交。
2. 曲库 `searchBox` 改为与成绩页相同的 `borderWidth + borderRadius + padding: 11`，去掉强制 `minHeight` / `lineHeight` / `textAlignVertical` 等样式。
3. 保留列表 `paddingTop: 12`（状态栏与筛选间隙）。

## 期望输出

曲库搜索框占位文字观感与成绩页一致。

## 实际输出

已回退覆盖层并按成绩页样式对齐搜索框。
