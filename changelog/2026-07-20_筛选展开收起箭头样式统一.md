# 筛选展开收起箭头样式统一

## 改动原因

筛选栏右侧「展开⌄ / 收起⌃」使用 Unicode 箭头字符，上下两种状态渲染粗细与形状不一致。

## 具体实现

在 `MaimaiFilterBar` 中新增 `CollapseToggleAction` 组件，文字与箭头分离，箭头统一使用 `Ionicons` 的 `chevron-down` / `chevron-up`，与项目内其他展开收起控件一致。

## 期望输出

筛选栏收起与展开时，右侧箭头视觉风格一致，仅方向不同。

## 实际输出

已实现上述统一，相关 UI 测试仍通过。
