# 按好友码分别保存上传目标勾选

## 改动原因

切换好友码后「上传到」勾选仍共用一份全局配置，无法为不同好友码记住各自目标。

## 具体实现

1. `upload-prefs-store` 升级 v2：`selectionsByFriendCode` 按好友码存勾选；兼容迁移 v1。
2. 切换历史好友码 / 输入满 15 位时恢复该码勾选；勾选变更写入对应好友码。
3. 删除历史 JWT 时同步 `removeSelection`；临时勾选仍不写入持久表。

## 期望输出

不同好友码各自保留「上传到」勾选；切换后自动恢复。

## 实际输出

同上；相关单测通过。
