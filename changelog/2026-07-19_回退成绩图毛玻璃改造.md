# 回退成绩图毛玻璃改造

## 改动原因

为导出兼容改写的毛玻璃方案把预览和导出观感都弄坏了，按用户要求整段回退到改坏前的原实现。

## 具体实现

- 恢复原先的三层 `backdrop-filter` 空层毛玻璃与 `--glass-opacity:0`。
- 删除姓名框克隆、`export-capture` 双轨样式及相关 JS/CSS。
- 保留与毛玻璃无关的导出布局稳定改动（导出视口 `scale(1)`、复用预览高度）。

## 期望输出

- 应用样式毛玻璃观感回到改造前。

## 实际输出

- `npm run typecheck` 通过；`best-image-html` 测试通过。
- 按 AGENTS.md 完成本次提交，未 push。
