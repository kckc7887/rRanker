# Phigros 评价筛选复用评价标签

## 改动原因

成绩页评价筛选项原先是纯文字下拉，与成绩卡片上的评价标签视觉不一致。

## 具体实现

1. 抽出共用 `PhigrosRateBadge` 与 `phigros-rate-theme`（颜色/文案与成绩卡一致）。
2. 成绩卡片、歌曲详情改用共用徽章。
3. 筛选「评价」改为横向芯片，直接渲染 `PhigrosRateBadge`（φ / FC / V / S / A / B / C / F）。

## 期望输出

评价筛选项外观与成绩列表评价标签一致，选中态以外框标识。

## 实际输出

已按上述接入；相关单元测试通过。
