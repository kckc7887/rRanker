# 上传页代理提示改为域名分流与直连流出

## 改动原因
用户指出不应把流量全堆在电脑上，需要设置流出（其余直连）。

## 具体实现
- `UploadDataSheet` 文案改为：仅 `tgk-wcaime.wahlap.com` / `maimai.wahlap.com`（可选 `*.wahlap.com`）走截包代理，其它 DIRECT；代理须能向上游转发；换 cookie 仍 NO_PROXY。
- `TODO.md` 同步「域名分流、避免整机流量堆电脑」。

## 期望输出
上传页明确分流与流出，不再暗示整机走电脑。

## 实际输出
提示文案已更新。
