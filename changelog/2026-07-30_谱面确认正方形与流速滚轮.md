# 2026-07-30 谱面确认播放区正方形与流速滚轮

## 改动原因

谱面确认页播放区被纵向拉满后，圆形判定区被 CSS 拉伸变形；流速滑条步进过粗，需要 0.1 精细调节。

## 具体实现

- `#canvas-stage` 由 JS 按播放区 `min(宽, 高)` 裁成正方形，canvas 仅在正方形内 100% 铺满，避免 backing store 被非等比拉伸。
- 流速控件改为纵向 Wheel Picker（3.0–9.0，步进 0.1），并重建 `assets/maimai-chart-preview` 内联产物。

## 期望输出

圆形谱面保持正圆；流速可滚动精确到 0.1。

## 实际输出

已重建 WebView 播放器产物；布局与滚轮逻辑写入 webview-player 源码。
