# 推分计算可切换是否包含 φ

## 改动原因

部分推荐目标 Acc 为 100%（需 φ），用户希望能一键排除这类谱面。

## 具体实现

1. `findPushRecommendations` 增加 `includePhi`（默认 true）；为 false 时跳过目标 Acc≥99.995% 的结果。
2. Provider / 推分页增加「包含 φ」开关，切换后重新计算。
3. 空态文案在已排除 φ 时提示可重新开启。

## 期望输出

关闭「包含 φ」后，列表中不再出现目标 Acc 100% 的推荐。

## 实际输出

单元测试覆盖排除逻辑；与期望一致。
