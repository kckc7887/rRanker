# 2026-07-26 机厅查找筛选器对齐

## 改动原因

机厅查找页筛选器为自绘样式，与成绩/曲库页的折叠筛选条不一致。

## 具体实现

1. 新增 `ArcadeFilterBar`：对齐 `MaimaiFilterBar` 的折叠摘要、重置/展开收起、行标签布局，复用 `NeutralChip`。
2. 机厅查找页搜索框改为成绩页同款 `searchArea` / `searchBox` 样式。
3. 筛选摘要逻辑放入 `arcade-shops` 并补充单测。

## 期望输出

筛选条视觉与交互与现有筛选一致；单测通过。

## 实际输出

`vitest` 相关用例通过。
