# 2026-07-26 机厅文案去除 HTML

## 改动原因

nearcade 店铺备注等字段含 `<b>` 等 HTML，详情/列表直接展示会出现标签原文。

## 具体实现

新增 `stripArcadeHtml`，在 nearcade 映射店铺与机台文本字段时去掉标签并解码常见实体；补充单测。

## 期望输出

界面只显示纯文本，不再出现 `<b></b>`。

## 实际输出

`vitest`：`arcade-shops`、`nearcade-client` 相关用例通过。
