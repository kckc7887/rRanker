# 设置页已用空间随焦点刷新

## 改动原因

存储管理入口的「已用」只在设置页首次挂载时统计；下载字体/资源或在详情页清缓存后返回，副标题不会更新。

## 具体实现

设置页改为 `useFocusEffect`：每次进入/回到设置页时重新 `collectStorageUsage` 并刷新文案。

## 期望输出

从存储管理返回、切换到设置 Tab、或清缓存后再看入口，已用空间与当前占用一致。

## 实际输出

设置页已改用焦点刷新；导航单测 mock 同步调用 `useFocusEffect`。
