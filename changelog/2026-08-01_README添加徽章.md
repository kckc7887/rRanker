# 2026-08-01 README 添加徽章

## 改动原因

按 review 建议，在 README 标题下添加 shields.io 静态徽章，提升项目信息可读性。

## 具体实现

- 在 `# rRanker` 标题下方、`</div>` 前添加三枚 flat-square 徽章：
  - License：`AGPL--3.0`，链接到 `LICENSE` 文件。
  - 平台：`Android | iOS`。
  - 技术栈：`React Native | Expo SDK 54 | TypeScript`。
- 空格与 `|` 使用 URL 编码，连字符使用 `--` 避免被 shields 解析为分隔符。
- 不添加版本号徽章（版本号只由用户决定）、CI 徽章（仓库无 CI）与游戏徽章（与 README 正文保持只列两个游戏的现状一致）。

## 期望输出

README 标题下方显示三枚徽章，渲染正常，无其它内容变动。

## 实际输出

三枚徽章已插入，其余内容不变。
