# 2026-07-26 机厅机型筛选改为同时包含

## 改动原因

多选游戏机类型时原先为「包含任一」；用户期望为「同时包含所选全部机型」的机厅。

## 具体实现

将 `shopMatchesGameTitles` 改为 AND：机厅 `games` 须覆盖每一个勾选的 `titleId`；更新单测。

## 期望输出

勾选舞萌 DX + 中二节奏时，只列出两者都有的机厅。

## 实际输出

`vitest`：`arcade-shops` 相关断言通过。
