# 上传目标改对齐 score-hub，并清空本地堆积改动

## 改动原因

1. 总览上传半成品已拆除，入口退回「待实现」。
2. 用户要求 M5 目标改为参考 `refer/maimai-score-hub`：DXNet Friend VS / rival → 快照 → 水鱼/落雪导出，不再走微信华立链路。
3. 本地长时间未提交的游戏管理、总览拆分、多游戏模型等改动堆积，统一入库。

## 具体实现

- 更新本地 `TODO.md` / `ROADMAP.md`（二者在 `.gitignore` 中，仅本机规划）：M5 改 DXNet 方向，废弃微信截包方案。
- 总览上传 Alert 文案改为 DXNet 方向说明。
- 一并纳入：游戏管理/账号切换、DxRating 主题、分游戏空数据、上传拆除与依赖清理、相关 changelog 与测试。

## 期望输出

远程仓库工作区干净；产品代码与 changelog 反映「上传待实现 + M5=DXNet 导出」；本机 `TODO`/`ROADMAP`/`refer/` 仍按 ignore 保留。

## 实际输出

与期望一致（规划文件因 gitignore 不进远程，方向已写入本 changelog）。
