# 本地服务：接到 callback 后真正执行换 cookie

## 改动原因
用户指出上传页只写了文案，没有「接到链接之后」的程序。

## 具体实现
- 新增 `demo/local-capture-service/`：
  - `server.py`：`POST /v1/capture` → `maimai_py` 换 cookie → 玩家名即停；`/v1/status` `/v1/result`
  - `mitm_forward_addon.py`：截到华立 callback 后 POST 本服务，并短路微信请求以免消费 code
  - `capture_logic.py` / `README.md` / `run.bat`
- App 上传页补充说明：11451=截包入口，18765=接到后的处理服务

## 期望输出
电脑跑 `server.py` + mitm 后，截到链接会自动出玩家名结果。

## 实际输出
本地处理服务已落地；成绩上传可随后再扩。
