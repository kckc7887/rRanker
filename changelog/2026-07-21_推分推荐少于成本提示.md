# 推分推荐少于成本时提示

## 改动原因

当可达份额的推荐谱面数少于期望成本歌数时，即使用完这些谱面也凑不满期望 RKS，需要明确提示用户。

## 具体实现

在 `push-rks.tsx` 列表标题下：若 `0 < recommendations.length < songCost`，用 warning 色提示「仅有 X 张少于期望成本 Y 首」。

## 期望输出

推荐数少于成本时出现警告文案；等于或超过成本时不提示。

## 实际输出

与期望一致。
