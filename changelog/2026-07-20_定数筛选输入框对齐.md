# 定数筛选输入框对齐

## 改动原因

成绩页筛选同时展示定数与达成率范围时，定数标签列宽（36）小于达成率（44），导致上下两行输入框左缘不齐。

## 具体实现

在 `MaimaiFilterBar` 中，当显示达成率筛选时，定数标签同样应用 `wideFilterLabel`（44px 宽），使两行范围输入框起始位置一致。

## 期望输出

成绩页展开筛选后，定数与达成率的上下限输入框左缘垂直对齐。

## 实际输出

已实现上述对齐，曲库页（无达成率筛选）仍保持原 36px 标签宽度。
