# 谱面确认播放按钮改为图标样式

## 改动原因
原播放/暂停按钮使用文字（"播放"/"暂停"），视觉效果不够简洁。

## 具体实现
1. `index.html`：按钮默认内容从文字改为 SVG 播放三角图标，新增 `#play` 样式（40×40 圆形按钮，flexbox 居中）。
2. `main.ts`：添加 `PLAY_ICON` / `PAUSE_ICON` SVG 常量，播放/暂停切换时通过 `innerHTML` 替换图标，同步更新 `aria-label`。

## 期望输出
圆形图标按钮：播放时显示 ▶，暂停时显示 ⏸。

## 实际输出
与期望一致。
