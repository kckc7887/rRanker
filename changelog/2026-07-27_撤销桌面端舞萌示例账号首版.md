# 撤销桌面端舞萌示例账号首版

## 改动原因

用户对提交 `3d785f0` 的桌面端舞萌示例账号首版不满意，并要求在该提交已经推送到远端后撤销本次实现。

## 具体实现

- 使用普通反向提交撤销 `3d785f0`，不改写已经共享的 Git 历史。
- 移除本批新增的桌面应用、共享核心和根级 workspace 配置。
- 恢复移动端原有领域模型、Provider、服务、配置、测试与独立锁文件。
- 清理桌面端构建和安装包生成目录。

## 期望输出

- 除本撤销记录外，受版本控制的产品代码恢复到 `3d785f0` 之前的状态。
- 远端通过普通 revert 提交完成回退，不使用强制推送。
- 手机端代码不再包含本批桌面端共享核心接线。

## 实际输出

- 反向变更无冲突应用，桌面端和共享核心文件已移除。
- 移动端源码、配置和锁文件已恢复到提交前状态。
- 提交前差异核对显示，除本撤销记录外，产品代码与 `3d785f0` 的父提交一致。
