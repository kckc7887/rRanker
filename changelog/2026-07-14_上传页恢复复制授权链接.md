# 上传页恢复「复制授权链接」

## 改动原因
用户澄清：只要去掉「粘贴最终回调」输入框，不应一并去掉复制授权链接（去微信打开）的按钮。

## 具体实现
- `UploadDataSheet` 恢复生成 + 复制华立微信授权链接（`generateWechatAuthorizeUrl`）
- 仍不提供粘贴最终回调 URL 的输入框；截包靠 mitm，App 只轮询状态

## 期望输出
上传页有「复制授权链接」主按钮 + 代理配置 + 爬取状态，无粘贴框。

## 实际输出
已恢复复制授权链接按钮。
