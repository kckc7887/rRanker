# 回退统一三游戏数据模型改造

## 改动原因

统一三游戏数据模型与共享页面改造使现有页面结构和维护关系变得更加混乱，未达到预期效果。根据用户要求，完整撤销提交 `28a766f`。

## 具体实现

- 反向撤销统一 JSON schema、内置 Manifest、三游戏适配器及跨文档校验。
- 恢复舞萌 DX、Phigros、中二节奏原有的总览、最佳、成绩、曲库和歌曲详情实现。
- 撤销通用筛选状态、共享页面组件、通用 `chartId`、SQLite schema 5 和备份 v4 改造。
- 移除本次改造新增的契约文档、JSON 示例与对应测试。

## 期望输出

- 工作代码恢复到提交 `28a766f` 之前的状态。
- 不保留本次统一模型改造引入的页面、数据和存储变化。
- 保持应用版本号不变，且不影响此前已有功能。

## 实际输出

- 除本回退记录外，代码树已与 `28a766f` 的父提交 `854a372` 完全一致。
- TypeScript 类型检查通过。
- 应用版本号未修改。
