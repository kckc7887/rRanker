# 好友码历史上拉改为下拉并可删除

## 改动原因

历史好友码用底部 Modal 不够直观，需要输入框旁下拉列表，并支持删除本地 JWT。

## 具体实现

1. `UploadDataSheet`：历史改为好友码输入行下方内联下拉；点「历史/收起」切换；每项可选择与「删除」。
2. `scoreHubAccountStore.remove`：删除指定好友码 JWT 条目。
3. 删除当前输入框对应好友码时，清除本地「已登录/已绑定」展示状态。

## 期望输出

点历史展开下拉列表，可切换好友码，也可删除条目。

## 实际输出

同上；相关单测通过。
