# Phigros 成绩筛选 Acc 文案与行距

## 改动原因

成绩页筛选「准确率」宜改为社区常用的 Acc；Acc 行与下一行评价间距过近。

## 具体实现

1. 筛选标签与收起摘要中的「准确率」改为 `Acc`。
2. Acc 行增加 `marginBottom: 6`，与评价行拉开间距。

## 期望输出

- 成绩页筛选显示 Acc。
- Acc 与评价两行间距明显大于原先贴合感。

## 实际输出

- 已按上述调整 `PhigrosFilterBar`。
