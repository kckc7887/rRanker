# 版本切换 iOS 点击失效修复

## 改动原因

歌曲详情页国服/日服版本切换功能在 iOS 端表现为：有视觉按压反馈（`switchPressed` 透明度变化），但 `onPress` 回调未触发，无法完成实际切换。

根因是 `VersionMetadataCell` 内嵌的 `AutoScrollText` 组件渲染了一个 `ScrollView`（horizontal 方向）。虽然外层 `View` 设置了 `pointerEvents="none"`，但 iOS 原生 `UIScrollView` 的手势识别器仍会在触摸系统中竞争，导致 `Pressable` 收到 `pressIn` 事件（触发视觉反馈）后，手势被 ScrollView 优先级抢占，`onPress` 被取消。

## 具体实现

**第一次尝试（未生效）：** `AutoScrollText` 新增 `scrollEnabled` 属性，禁用内部 `ScrollView` 用户触摸但保留 `scrollTo` 自动滚动。验证后发现仅设置 `scrollEnabled={false}` 仍无法阻止 iOS 原生 UIScrollView 手势干扰。

**第二次尝试（当前方案）：** `scrollEnabled={false}` 时 `AutoScrollText` 直接返回 `View` + `Text`（无 ScrollView），彻底从组件树中移除原生 UIScrollView。所有 Hook 调用保持无条件，仅 return 语句分支。

## 期望输出

- iOS 端点击版本区域可正常在国服/日服名称间切换
- 版本名称过长时在单元格内截断显示（无走马灯），不影响核心切换功能
- 其他交互（收藏、练习清单、标签编辑）不受影响

## 实际输出

- `npm run typecheck` 通过
- `npm run lint` 通过，0 错误、0 警告
- Vitest 单元测试 25 文件 93 测试全部通过
- Jest UI 测试 6 套件 23 测试全部通过，其中版本切换测试（第 84 行）断言正常
