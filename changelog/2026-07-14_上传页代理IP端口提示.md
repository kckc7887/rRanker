# 上传页展示截包代理 IP 与端口

## 改动原因
用户确认 Clash/Shadowrocket 等可导流，要求手机上传页提示代理 IP 和端口以便查看配置。

## 具体实现
- 安装 `expo-network`，读取本机局域网 IP
- `UploadDataSheet` 顶部增加代理卡片：可编辑 IP/端口（默认端口 11451）、大字号 `IP:端口`、复制与刷新
- 标明 Expo Go 不能本机开 mitm，代理应指向跑截包服务的机器

## 期望输出
打开「上传数据」即可看到代理 IP、端口与复制入口。

## 实际输出
上传弹出页已展示代理提示区。
