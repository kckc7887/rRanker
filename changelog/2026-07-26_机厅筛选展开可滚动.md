# 2026-07-26 机厅筛选展开可滚动

## 改动原因

机厅查找筛选展开后机型选项过多，内容超出屏幕且无法上下滑动，底部选项不可达。

## 具体实现

1. `ArcadeFilterBar` 展开区用 `ScrollView` 承载筛选内容，`maxHeight` 约为窗口高度 52%。
2. 机厅查找页结果区包一层 `flex: 1`，避免筛选撑满后列表无法占位。

## 期望输出

展开筛选后可在筛选区域内上下滚动查看全部机型。

## 实际输出

展开筛选内容受高度限制并可滚动；搜索框与「收起」仍固定在筛选顶部。
