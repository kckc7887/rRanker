# 机厅收起筛选隐藏 GPS 原点

## 改动原因

原点为当前位置时，收起筛选摘要若显示逆地理编码地址会显得冗余，用户只想在自定义原点时看到地址。

## 具体实现

`ArcadeFilterBar` 收起摘要仅在 `origin.source === 'custom'` 时附带原点标签；GPS 原点不写入摘要。

## 期望输出

当前位置时收起摘要为「距离 · 机型」；搜索地址设原点后摘要前缀为该地址。

## 实际输出

与期望一致。
