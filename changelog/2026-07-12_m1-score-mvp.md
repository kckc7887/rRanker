# M1 双端查分 MVP 交付

## 改动原因

M0 数据底座已交付，按已批准的 spec（`.trae/specs/m1-score-mvp/`）实施 M1 双端查分 MVP，把 M0 的 fixture 单例 + `useEffect`/`useState` 改造为 Zustand session store + TanStack Query 驱动的完整查分 MVP，新增查歌 tab 和成绩筛选，统一 5 态展示。

## 具体实现

- **Task 0**（e6e6597）：M1 开工基线 commit，TODO.md 标记已开工。
- **Task 1**（688cf9a）：安装 `@tanstack/react-query` v5 + `zustand` v5，新建 `src/state/query-client.ts`（staleTime 5m, retry 1, refetchOnWindowFocus false），在 `app/_layout.tsx` 注入 `QueryClientProvider`。
- **Task 2**（7c32a17）：新建 `src/state/session-store.ts`，Zustand store 保存 `ProviderSession` + `ScoreProvider`，未登录回落 `FixtureProvider`，`setSession` 切换 `DivingFishProvider`。
- **Task 3**（8395b43）：在 `src/fixtures/sanitized.ts` 新增 `fixtureSongs`（8 条边界：曲名重复、日文长标题、缺 artist、未知 version、DX/SD），`FixtureProvider.getSongs()` 返回非空。
- **Task 4**（89013b6）：新建 `src/hooks/use-score-snapshot.ts` 和 `use-songs.ts`，queryKey 含 session 标识，session 变化自动重新拉取。
- **Task 5**（e358202）：新建 `src/components/QueryStateView.tsx`（5 态：loading/error/empty/stale/data）和 `Card.tsx`。
- **Task 6**（04d75e3）：5 tab 结构（总览/B50/成绩/查歌/设置），新建 `app/(tabs)/search.tsx`（曲名/songId 模糊搜索 + `QueryStateView`）。
- **Task 7**（0b99ab7）：总览/B50/成绩/设置页全部切换到 `useScoreSnapshot` + `QueryStateView`；成绩页新增筛选栏（难度/版本/类型/排序）进 Zustand（`src/state/records-filter.ts`）；设置页接入 session store，登录成功后 `invalidateQueries` 触发全局刷新，新增会话状态显示和 Import-Token 获取路径文案；删除 `src/features/wireframe-data.ts`。
- **Task 8**（0142048）：`docs/api-protocol.md` 回填 `last_verified: 2026-07-12`，标注 `/query/player` 不复验；`docs/development-readiness.md` 第 2.C 节标注 UI 设计输入延后到 M2/M4。
- **Task 9**（b38aec4）：新增 4 个测试文件（search-filter 5 项、session-store 4 项、records-filter 6 项、query-state-view 5 项 skip），抽取 `src/utils/search.ts` `filterSongs` 纯函数；`npm test` 25 项全过。
- **Task 10**：本 changelog + TODO.md 标记已交付 + commit。

## 期望输出

- 5 tab 双端查分 MVP 可在 iPhone Expo Go 与 Android Emulator 运行。
- 同一 fixture 下数值、排序、筛选、页面状态一致。
- 未登录用 fixture，登录水鱼后自动切换 provider 并刷新。
- `npm test` / `typecheck` / `lint` 全过。

## 实际输出

- 10 个 task 各对应一次 git commit（e6e6597 → b38aec4）。
- `npm test`：6 文件 25 项全过（现有 10 + 新增 15，另有 5 项组件测试因 vitest node 环境限制 skip）。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- 双端运行时验收待用户在 iPhone Expo Go + Android Emulator 手动执行 `docs/mobile-testing.md` 第 4 节 M1 验收清单。
