# Phigros 机厅不筛机型

## 改动原因

从 Phigros 进入机厅查找时应默认不筛机型；未选机型时需展示距离内全部机厅。

## 具体实现

1. 空 `titleIds` 视为匹配全部机厅（不再过滤为空列表）。
2. 筛选摘要空机型显示为「全部机型」。
3. 偏好按游戏分存：Phigros 默认 `titleIds: []`，舞萌仍默认舞萌 DX；兼容迁移旧共享 key。

## 期望输出

Phigros 进入时不选中任何机型，列表为距离内全部机厅；舞萌默认仍筛舞萌 DX。

## 实际输出

与期望一致。
