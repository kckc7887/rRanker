# 回退搜索框占位居中相关改动

## 改动原因

多次调整搜索框占位垂直位置仍不满意；用户要求全部回退，恢复原先可用的写法。

## 具体实现

1. 曲库/成绩搜索框恢复为原来的 `padding: 11`。
2. 删除共用 `search-box-style.ts`。
3. 保留曲库列表 `paddingTop: 12`（状态栏与筛选间隙，属另一项需求）。

## 期望输出

搜索框观感回到居中实验前的状态。

## 实际输出

已回退搜索框样式相关改动。
