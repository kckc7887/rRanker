# 上传完成小字 5 秒后归零

## 改动原因

上传完成后总览按钮小字一直停在「上传完成」，需要片刻后回到默认「好友码」。

## 具体实现

`UploadDataSheet.applyPhase`：进入 `done` 后启动 5 秒定时器，回调 `idle` 并通知 `onPhaseChange`；新阶段或卸载时清除定时器。

## 期望输出

完成态展示约 5 秒后，按钮小字恢复为「好友码」。

## 实际输出

同上；单测覆盖定时归零。
