# 2026-07-12 M0 交付确认与 M1 事前准备

## 改动原因

用户确认 M0 验证通过允许交付，要求开始 M1 双端查分 MVP 的事前准备。本次变更只做状态标记、开工清单梳理与设计需求清单产出，不写 M1 业务代码。

## 具体实现

- 在 `docs/development-readiness.md` 第 6 节勾选全部 M0 完成标准，标注 iPhone Expo Go 与 Android Emulator 运行时由用户手动验收并通过，标记 M0 已交付、M1 正式开工。
- 在 `TODO.md` 末尾追加"M1 双端查分 MVP 事前准备"段落，按 AGENTS.md 要求的格式记录：用户原始意图、目标、M1 验收项、现状差距盘点表、实现思路、依赖/风险、当前优先级、暂缓原因。
- 现状差距盘点覆盖页面清单、fixture 数据、provider 注入、状态管理、水鱼端点、状态展示、测试覆盖、双端运行时、UI 设计输入共 9 个维度。
- 通过 AskUserQuestion 收口 4 个 M1 关键决策点：
  - 页面结构 = 5 tab（总览 / B50 / 成绩 / 查歌 / 设置），总览同时承担"首页+玩家概览"不拆分。
  - UI 视觉 = 用户要求先产出"M1 设计需求清单"再开工。
  - 状态管理 = M1 立即引入 TanStack Query + Zustand。
  - 水鱼 `/query/player` = 不复验，B50 用本地 `buildBest50` + `/player/records`。
- 把 4 个决策回写到 `TODO.md` 的差距盘点表、实现思路与依赖/风险段落；实现思路从 9 步调整为 10 步（新增"UI 设计输入落地"作为第 2 步硬门禁）。
- 新建 `docs/m1-design-brief.md`，按 12 节产出 M1 设计需求清单：视觉方向、配色、字体、间距、组件、5 个页面布局、状态展示、列表性能、平台差异、暗色模式、图标封面、动效；每节给"默认建议"+"待用户确认点"。
- 不修改任何 `apps/mobile/` 下的代码、配置或依赖。

## 期望输出

- M0 在文档层面正式标记为已交付。
- M1 开工前所有需要确认的决策点集中在 `TODO.md` 一处，后续实现按 10 步顺序推进。
- 4 个关键决策点已确认并回写。
- 用户审阅 `docs/m1-design-brief.md` 逐项确认后，M1 才进入 fixture 补全步骤。

## 实际输出

- `docs/development-readiness.md` 第 6 节全部勾选，并追加"M0 已于 2026-07-12 交付"结论。
- `TODO.md` 追加 M1 事前准备清单，包含差距盘点表（9 行）、10 步实现思路、依赖/风险（7 条）。
- `docs/m1-design-brief.md` 新建，13 节设计需求清单。
- 本 changelog。
- 未触碰 `apps/mobile/` 任何源码、配置或依赖；`npm test` / `typecheck` / `lint` 状态与 M0 交付时一致（10 项测试全通过）。
