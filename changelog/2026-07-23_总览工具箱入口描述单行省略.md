# 总览工具箱入口描述单行省略

## 改动原因

总览页「工具箱」入口的工具列举文案较长时会换行，影响卡片信息密度与扫读。

## 具体实现

在 `apps/mobile/app/(tabs)/(overview)/index.tsx` 的工具箱摘要 `Text` 上设置 `numberOfLines={1}` 与 `ellipsizeMode="tail"`；并补充总览相关测试断言。此前误改工具箱内列表的改动已回退。

## 期望输出

总览「工具箱」卡片中的工具列举最多显示一行，超出部分以省略号截断。

## 实际输出

工具箱入口摘要已限制为单行尾部省略；相关测试通过。
