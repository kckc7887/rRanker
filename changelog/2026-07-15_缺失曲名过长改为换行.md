# 缺失曲名过长改为换行

## 改动原因

自动滚动体验不佳，改为曲名过长时自然换行。

## 具体实现

1. 回退共用 `AutoScrollText` 抽取与缺失卡滚动接入。
2. 歌曲详情页恢复本地 `AutoScrollText`。
3. 缺失卡曲名使用可换行的 `Text`。

## 期望输出

曲名过长时在卡片内换行显示，不再横向滚动。

## 实际输出

自动滚动已回退；缺失曲名改为换行。
