# 筛选下拉取消淡入淡出

## 改动原因

成绩页与曲库页的版本、单人成就、多人成就三个锚点下拉使用 Modal `fade` 动画，展开收起淡入淡出偏慢，视觉上拖沓。

## 具体实现

将 `FilterAnchoredDropdown` 的 Modal `animationType` 由 `fade` 改为 `none`，展开收起立即切换。

## 期望输出

三个下拉列表打开与关闭无淡入淡出延迟，响应更干脆。

## 实际输出

已改为无动画切换。
